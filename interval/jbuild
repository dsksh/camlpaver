(jbuild_version 1)

(library
  ((name interval)
   (c_names (stub))
   (c_flags (-I.))
   (c_library_flags (-L${SCOPE_ROOT}/../interval -lwrapper -lstdc++)) ; TODO
   (preprocess (pps (ppx_inline_test)))
   (inline_tests)
))
